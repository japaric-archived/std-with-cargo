language: rust
rust: nightly

env:
  matrix:
    - BACKTRACE=no JEMALLOC=no
    - BACKTRACE=yes JEMALLOC=no

install:
  - source .travis.install.sh

script:
  - ./.travis.script.sh

branches:
  only: master

notifications:
  email:
    on_success: never
